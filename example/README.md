# flutter_xlog example

This example initializes `flutter_xlog`, writes sample logs, flushes them, and
closes the native logger.

Run it from the example directory:

```bash
flutter pub get
flutter run
```
